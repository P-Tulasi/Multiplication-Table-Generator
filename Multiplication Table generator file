# Ask the user to enter a number
number = int(input("Enter a number: "))

# Loop from 1 to 10
for i in range(1, 11):
    # Print the multiplication result using formatted string
    print(f"{number} x {i} = {number * i}")
